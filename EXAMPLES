;; This is a comment
;; Code Examples

(function_name arg1 arg2)

(defun fib (n)
  (if (< n 2)
      n
      (+ (fib (- n 1))
         (fib (- n 2)))))

(lambda (args...)
    ;; code here ...
)

(defun list va_args
    (memcpy
        (malloc #args)
        va_args
        #args)
    ))

(struct Object
    .a = "field 1"
    .b = #true
    .c = #args
)
