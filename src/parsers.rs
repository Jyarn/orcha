pub enum Error {
    Overflow,
    InvalidSyntax,
}

pub fn parse_i64(str: &str) -> Result<i64, Error> {
    let mut accum: i64 = 0;
    return Ok(accum);
}

pub fn parse_u64(str: &str) -> Result<u64, Error> {
    let mut accum: u64 = 0;

    return Ok(accum);
}
